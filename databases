-- docker
    cd /var/www/databases

    docker-compose up -d
    docker exec -it databases /bin/bash

-- Acceder a bd mysql
  mysql -h databases -u root -p


    CREATE DATABASE alumnos;


-- Create usuario
    GRANT ALL ON ceb.* TO 'ceb'@'localhost' IDENTIFIED BY '' WITH GRANT OPTION;
